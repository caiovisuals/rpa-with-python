# Copie para .env e preencha. O .env NUNCA é versionado.
# Este arquivo cresce conforme as fases avançam; hoje o projeto é só domínio puro
# e ainda não lê nenhuma dessas variáveis.

# Ambiente: development | test | production
APP_ENV=development

# --- pendente da decisão D2 (onde o sistema roda) -------------------------
# DATABASE_URL=
# SECRET_KEY=
# BANK_DATA_ENCRYPTION_KEY=
